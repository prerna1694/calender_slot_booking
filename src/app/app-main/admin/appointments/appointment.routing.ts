import {Routes} from '@angular/router';
import {ShowAppointmentComponent} from './show-appointment/show-appointment.component';

export const AppointmentRouting: Routes = [
  { path: 'show', component: ShowAppointmentComponent}
];
