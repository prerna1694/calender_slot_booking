import {Routes} from '@angular/router';
import {ShowScheduleComponent} from './show-schedule/show-schedule.component';

export const ScheduleRouting: Routes = [
  { path: 'show', component: ShowScheduleComponent}
];
